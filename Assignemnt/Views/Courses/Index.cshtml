@model IEnumerable<Assignemnt.Models.Course>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CrsName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LectHours)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LabMinutes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CrsName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LectHours)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LabMinutes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CrsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CrsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CrsId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
